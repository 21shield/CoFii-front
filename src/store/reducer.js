
const defaultState = {
    currentUser: "",
    // position:[long,lat]
};


const reducer = (state = defaultState, action ) => {
    switch(action.type){
        // case
        default:
            return state
    }
}

export default reducer